eureka:
  instance:
    hostname: localhost # 表示主机名称
  client:
    registerWithEureka: false # 是否注册到服务器
    fetchRegistry: false # 是否获取服务器注册列表
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ #域名地址

spring:
  application:
    name: eureka-server # 微服务名称
