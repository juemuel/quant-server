spring:
  application:
    name: config-server
  #  profiles: # 使用本地
  #    active: native
  cloud:
    config:
      server: # 可以设置保存git、svn或本地
        #        native: # 本地保存位置
        #          search-locations: classpath:/java
        git: # 使用git，失败propertySources 为空
          uri: https://github.com/juemuel/server-config.git
          default-label: main
          search-paths: java
          username: 799525644@qq.com
          password: jue1029github
          skip-ssl-validation: true
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/